#include "main.h"

/**
 * _isupper - checks for uppercase
 * @c: character
 *
 * Return: 1 if true or 0 if not
 */

int _isupper(int c)
{
	int i;

	i = 'a';
	for (; l <= 'z'; i++)
		if (c == i)
			return (1);
	return (0);
}
